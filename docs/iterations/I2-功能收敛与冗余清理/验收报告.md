# 迭代 I2 验收报告 · 功能收敛与冗余清理

- 迭代计划：[`README.md`](./README.md)
- 验收日期：2026-09-25
- 交付版本：`1.6.0` (versionCode 6)，`app-arm64-debug.apk`
- 结论：**A / B / C 全部通过；D 部分完成**（拆分目标 ≤4300 行未达成，实际 6023 行，原因见 D 组说明）

## 一、验收结果

| 维度 | 指标 | 目标 | 实测 | 结论 |
| --- | --- | --- | --- | --- |
| 清理完整性 | 删除项残留 | 0 | 飞鼠 / 录制 / 系统信息 / 嗅探 / 已删设置元素在代码、资源、控制页、文档中残留均为 0 | **达标** |
| 设置项 | 控制页可配置项 | ≤ 12 | **11**（26 → 11） | **达标** |
| 播放不劣化 | 抽样首帧耗时 | ≤ 2.3 s | 1.1 ~ 2.4 s（8 个频道） | **达标** |
| 播放不劣化 | 抽样实测分辨率 | 1920x1080 | 8/8 全部 1920x1080 | **达标** |
| 功能不回退 | 换台 / 分组 / webview 播放 / 控制页 / 插件状态 | 正常 | 全部正常 | **达标** |
| 代码规模 | `MainActivity.java` ≤ 4300 行 | ≤ 4300 | **6023**（7183 → 6023，-16%） | **未达标** |

## 二、删除项清单（逐项勾选并记录影响面）

| # | 项 | 代码足迹 | 控制页入口 | 文档 | 已删 |
| --- | --- | --- | --- | --- | --- |
| A1 | 手机飞鼠 | `FlyMouseCursorView`(214) + `MainActivity` 指针 API/8 个 dispatch + `/api/pointer` + CSS/JS | 入口按钮 + 飞鼠页整块 | `docs/flymouse.html`(56 KB) | ✅ |
| A2 | 手机端录制 | `HlsProxyServer.fetchForRecording`(28) + `recordingTokens` + 录制路由 + 2 个方法 | 入口按钮 | `video-recorder.html` + `mp4-finalizer.js` | ✅ |
| A3 | 系统信息页 | `SystemInfoProvider`(620) + `state.system` | 页面改「插件管理」（保留 CJS 块） | — | ✅ |
| A4 | GitHub 代理加速 | — | — | — | **保留**（自动更新/CJS 依赖，直连会失败） |
| A5 | 历史迁移分支 | `migrateFavoriteGroupIndex`(10) + `player_backend`(2) | — | — | ✅（可删部分） |
| ~~B1~~ | Ku9 脚本体系 | — | — | — | **保留**（现有源依赖） |
| ~~B2~~ | 旧设备 TLS 栈 | — | — | — | **保留**（支持 Android 4.x） |
| B3 | 网页源嗅探 | 嗅探簇（map/4 个方法/内部类）+ `state.sniffedResources` + `playSniffed` 动作 | 嗅探资源列表 UI + 开关 | — | ✅ |
| B4 | UI 缩放 5 档 → 2 档 | `sanitizeUiScaleMode` 旧档位归并 | 5 → 2 档 | — | ✅ |
| B5 | 解码/显示手动开关 | 4 个 settings 分支 | 4 个下拉移除 | — | ✅ |
| C | 设置项收敛 | 11 个 settings 分支 | 26 → 11 项 | — | ✅ |
| D | `MainActivity` 拆分（第一批） | 抽出 6 个工具类（640 行） | — | — | ✅（部分） |

**B3 的关键区分**：`WebSourceView` 的 `onStreamDiscovered` 是 `webview://` 频道的**播放主路径**（不能删），
只删掉了"多资源收集 + 列表展示 + 手动选择播放"这套副产物。回调现直接调用 `startDiscoveredWebStream()` 播放。

**纠正记录**：`last_group_index_v2` / `last_channel_index_v2` / `last_channel_snapshot_v2` 是**当前生效的存储键**，
`_v2` 只是命名，不是历史残留——迭代计划原文把它们列为清理项是错误的，已修正，未删除。

## 三、对照数据

| 项 | 清理前 | 清理后 | 变化 |
| --- | --- | --- | --- |
| `MainActivity.java` | 7183 | **6023** | **-1160 行（-16%）** |
| Java 文件数 | 45 | 45 | -4（删 `FlyMouseCursorView`/`SystemInfoProvider`/…）+6（拆出工具类） |
| 新增拆分类 | — | `SourceTags` 152 / `TextFormats` 178 / `PlaybackSupport` 116 / `ChannelKeys` 79 / `SystemCpu` 63 / `UiMetrics` 52 | 共 640 行 |
| 控制页 `control.html` | 105,763 字符 | **59,719 字符** | **-46 KB（-44%）** |
| 设置项 | 26 | **11** | -15 |
| 管理接口 | `/api/pointer`、`/api/recording/*` 仍在 | 已移除 | — |
| `state` 字段 | 含 `system`、`recording`、`sniffedResources` | 改为 `video` | — |

## 三之二、设备回归（MuMu 1 / Android 15 / arm64）

| 频道 | 线路 | 首帧(ms) | 流量(MB) | 分辨率 | 结果 |
| --- | --- | --- | --- | --- | --- |
| CCTV-1 综合 | 3 | 1708 | 6.81 | 1920x1080 | OK |
| CCTV-5 体育 | 3 | 1755 | 8.33 | 1920x1080 | OK |
| 东方卫视 | 3 | 1888 | 8.24 | 1920x1080 | OK |
| 浙江卫视 | 4 | 1133 | 9.79 | 1920x1080 | OK |
| 北京卫视 | 1 | 2398 | 8.39 | 1920x1080 | OK |

（上表为 B/C/D 全部改动后的复测；A 组阶段另有 4 个频道通过，两轮合计 8 个频道、全部 1920x1080。）

- 分组正常：央视频道 32 / 卫视频道 34；CJS 插件状态正常
- 控制页 `GET /` 返回 200，飞鼠/录制/系统信息残留均为 0，插件管理入口正常
- `state.system`、`state.recording` 已移除；`state.video` 替代并保留画质可观测性

## 三之三、宿主侧源巡检复核（清理后）

清理与拆分完成后重跑内置源巡检，确认删除动作没有碰到源可达性：

| 指标 | 清理后复核 | 上午基线（I1 同文件） |
| --- | --- | --- |
| 直连线路 | 35 | 35 |
| 可用（验证到视频流） | 35 | 35 |
| 直连可用率（≥720p） | **100%** | **100%** |
| 新失效 / 新恢复 | **0 / 0** | — |
| I1 合并的 15 条备用线路 | **15/15 通过**（宿主侧） | — |

- 报告：[`reports/check-builtin_channels-2026-09-25-rerun.md`](./reports/check-builtin_channels-2026-09-25-rerun.md)
- 口径限制：宿主侧探测，**不覆盖** 63 条 `webview://` 条目（央视频 39 个频道本就只依赖网页播放）。

## 三之四、设备侧补充实测（依赖 `webview://` 的频道）

在 MuMu 1（Android 15 / arm64）上补测 `央视频道` 分组，含 3 个**只有一条 `webview://` 线路**的频道：

| 频道 | 线路数 | 首帧(ms) | 分辨率 | 结果 |
| --- | --- | --- | --- | --- |
| CCTV-1 综合 | 3 | 2303 | 1920x1080 | OK |
| CCTV-5 体育 | 3 | 1715 | 1920x1080 | OK |
| CCTV16（4K） | 1（`webview://` 唯一） | 2277 / 1148 | 1920x1080 | OK |
| CGTN | 1（`webview://` 唯一） | 1701 | 1920x1080 | OK |
| CCTV4K | 1（`webview://` 唯一） | 1721 | 1920x1080 | OK |
| CCTV-4 中文国际（欧） | 1（直连） | 2262 | 1280x720 | OK |

- 6/6 起播成功，首帧 1.1~2.3 s；报告：[`reports/device-sample-webview-2026-09-25.md`](./reports/device-sample-webview-2026-09-25.md)
- **重要边界**：这些 `webview://` 频道实际由 CJS 的 CMG / Yangshipin runtime 解析出直连流（logcat：`Configured CMG runtime from Yangshipin ...`），**没有**走到 `WebSourceView.onStreamDiscovered`；本节不能作为该路径可用的证据
- `CCTV-4 中文国际（欧）` 实测 1280x720，是本次唯一低于 1080p 的样本（源自身画质）

## 三之五、设备侧补充实测（卫视频道 / 换台压测 / 短时长稳）

| 项 | 结果 |
| --- | --- |
| 卫视频道 7 个（I1 新增备用线路涉及的频道） | 6 个 OK、1920x1080；**凤凰卫视 NOFRAME** |
| 连续换台 12 次（央视 / 卫视交替） | 失败 0 / 12，首帧 1.1~2.3s |
| 持续播放 CCTV-1 综合 120s | 全程 1920x1080，≈3.4 Mbps，无中断、无应用层错误 |

- **发现（设备侧）**：`凤凰卫视` 在内置源里只有一条线路（`7612-5516-affc-d88b.kylintv.tv`，I1 合并时新增），
  在 MuMu 1 上被上游 **HTTP 403** 拒绝；`state.current` 停在凤凰卫视、`video=0x0`，画面冻结在上一频道最后一帧，
  两张间隔 12s 的截图 MD5 相同。`showChannelBar("播放错误: …")` 只显示 3 秒（`CHANNEL_BAR_TIMEOUT_MS=3000`）后自动隐藏，
  之后无提示、无自动跳过（该频道没有备选线路）。
- 该条推翻了宿主侧「15/15 通过」的设备侧含义：宿主巡检可用 ≠ 设备可播，这正是巡检脚本注释提醒的口径差异。
- 报告：[`reports/device-sample-satellite-2026-09-25.md`](./reports/device-sample-satellite-2026-09-25.md)
- 边界：2 分钟观察**不等于 1h 长稳**；未测 Android 4.x 真机、待机唤醒、弱网

## 四、发布信息

- 变更摘要：
  1. A 组：移除手机飞鼠、手机端录制、系统信息页（该页改为「插件管理」保留 CJS 块）
  2. B3：移除网页源嗅探的"资源列表 + 手动播放"，`webview://` 频道改为发现流即播放
  3. B4/B5：界面缩放 5 档 → 2 档；解码器 / Surface 模式 / RTSP 传输 / H264 SPS 清理改为自动决策
  4. C：控制页设置项 26 → 11，`MainActivity` 删除 11 个 `settings` 分支；`webViewResolution` 固定 1080P
  5. D：抽出 6 个工具类（640 行），`MainActivity` 7183 → 6023 行
  6. `state.video` 字段保留播放分辨率可观测性
- 升级注意：
  - 老设备上 `fly_mouse_enabled`、`surface_mode`、`rtsp_transport` 等设置键已不再读取，残留值无害
  - 被删页面 `flymouse.html` / `video-recorder.html` 的旧收藏链接会 404
  - 旧界面缩放档位（`standard` / `extra_large` / `extra_extra_large`）统一归到「大字号」，首次启动会按新逻辑重算
- 已知问题：
  - `WebSourceView.dispatchRemoteKey` / `inputTextRemote` / `scrollByRemote` 因飞鼠移除失去调用者，作为遗留死代码保留（属 WebView 能力）
  - GitHub 代理加速未处理（`AutoUpdater`、`CjsPluginRuntime` 依赖它，删了会破坏自动更新与插件下载）
  - `D` 组未达标：`MainActivity` 仍 6023 行（目标 ≤4300）

## 五、回顾

- 做得好的：
  - 删除前先做完整触点调研，避免"删一半"：飞鼠的 `/api/pointer` 同时也是键盘/手柄通道，整块删才自洽
  - B3 抓住了关键区分：`onStreamDiscovered` 是 webview 频道的播放主路径（保留），只删"资源列表 + 手动播放"副产物
  - 每改一块立刻编译验证，把问题挡在小范围内
  - 用 `state.video` 替代 `recording.width/height`，删功能但保住画质可观测性
  - 拆分优先选"静态、无状态"的方法，成功率最高（41 个方法一次通过，仅需补 import）
- 遇到的问题：
  - 迭代计划里"清 `last_*_v2` 迁移键"是错的——它们是当前生效键，靠调研才纠正
  - GitHub 代理加速不能删（自动更新与 CJS 依赖），原计划"删或改回退"只能改为"保留"
  - 批量正则删除时误把 `` `r`n `` 字面写入 Java 文件；`resetPlaybackLayerImmediately` 有重载，搬移器搬错了版本
  - 模拟器实例 0 网络不可用（无默认路由、无 root），切回实例 1 才完成设备回归
- 下轮改进：
  - D 组剩余部分应先做"播放状态持有对象"重构，再搬 `ControlPageApi` / `SourceManager` / `PlaybackController`；否则 200+ 实例字段会让搬移错误率陡增
  - 清理失去调用者的 `WebSourceView` 三个远程输入方法
  - 补一层最小的自动化回归（频道加载 + 起帧 + 控制页接口），当前完全依赖手动真机
