org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=false
android.enableJetifier=false

# 本文件会提交到仓库：禁止写入诊断接收地址、加签密钥等任何凭据。
# 需要注入时请用命令行参数（仅自用构建）：-PdiagnosticWebhook= / -PdiagnosticSecret=

